# Makefile simples de exemplo
PORTNAME=hello-world
DISTFILE=http://example.org/hello-world-1.0.tar.gz

all:
	@echo "build placeholder for $(PORTNAME)"

install:
	@echo "install placeholder into $(PREFIX)"
